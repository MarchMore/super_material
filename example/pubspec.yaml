name: example
description: 'A new Flutter project.'

publish_to: 'none'

version: 0.0.1

environment:
  sdk: '>=2.16.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  super_material: ^0.0.1

  cupertino_icons: ^1.0.4

dev_dependencies:
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true